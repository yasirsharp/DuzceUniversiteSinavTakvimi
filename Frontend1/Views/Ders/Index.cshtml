@using Business.Concrete
@using DataAccess.Concrete
@using Entity.Concrete

@model List<Ders>

@{
    ViewData["Title"] = "Ders Yönetimi";
    void getAllBolum()
    {
        <tbody id="tableBody">
            @foreach (var item in Model)
            {
                <tr data-id="@item.Id">
                    <th scope="row">@item.Id</th>
                    <td>@item.Ad</td>
                    <td>
                        <button class="btn btn-warning btn-sm edit-btn" data-id="@item.Id" data-name="@item.Ad">Düzenle</button>
                        <button class="btn btn-danger btn-sm delete-btn" data-id="@item.Id" data-name="@item.Ad">Sil</button>
                    </td>
                </tr>
            }
        </tbody>
    }
}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center">
        <h2>@ViewData["Title"]</h2>
        <button id="addRowBtn" type="button" class="btn btn-secondary">Ekle</button>
    </div>
    <table class="table table-bordered mt-3" id="dataTable">
        <thead class="table-light">
            <tr>
                <th style="width: 20%;">Id</th>
                <th>Ders Adı</th>
                <th>İşlem</th>
            </tr>
        </thead>
        <tbody id="inputRow" style="display: none;">
            <tr>
                <th scope="row">#</th>
                <td>
                    <input type="text" id="newAd" class="form-control" placeholder="Ders adı giriniz">
                </td>
                <td>
                    <button class="btn btn-success btn-sm" id="submitBtn">Kaydet</button>
                    <button class="btn btn-secondary btn-sm" id="cancelNewRowBtn">İptal</button>
                </td>
            </tr>
        </tbody>
        @{
            getAllBolum();
        }
    </table>
</div>
@section Scripts {
    <script src="~/js/component-ders.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Sayfanın türüne göre entityName'i belirliyoruz
            const entityName = 'Ders'; // Bu sayfa için 'Bolum' nesnesi

            initializeDeleteButtons(entityName);
            initializeAddButton(entityName);
            initializeEditButtons(entityName);
        });
    </script>
}